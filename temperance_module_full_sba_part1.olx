<!--
---
description: "READ 1000 — Temperance (FULL SBA): the complete Temperance module, extending the storyboard's authored portion through the parts it left 'in progress'. Across five parts, a study group (peers Danielle, Joe, Karis; reading instructor Professor Booker) works through three texts — an American Yawp textbook excerpt, N. Currier's 1846 lithograph 'The Drunkard's Progress', and a magazine article the instructor hands out on paper — building toward PULLING IT TOGETHER, a four-or-five-sentence synthesis of all three texts, by rating vocabulary, close-reading and highlighting, checking a peer's notes, answering comprehension questions, and analyzing the image, then finally writing four or five sentences comparing the historic temperance movement (the 1800s) with today's approaches to drinking less. Tabbed design: a Main Content tab holding the scripted conversation beside the activity pane — the conversation opens each activity into the pane with a target embed, which also leaves a labelled, clickable marker in the transcript at the point the activity happened — then one tab per reading (Professor Hale's lecture, the four American Yawp parts, the lithograph), and the Pulling It Together tab. Companion to the storyboard-faithful build (sba/temperance_module_sba)."
category: writing
index: 56
lang: en-Latn-US
---
-->

<!--
  READ 1000 — Temperance — FULL SBA (Part 1)

  The COMPLETE Temperance build. It shares Parts 1-3 with the storyboard-faithful
  build (sba/temperance_module_sba) and continues PAST the storyboard's "rest is
  in progress" slide:
    * Part 3 (Main Readings) — the textbook (vocabulary, Benevolent Empire, close
      reading, comprehension), then the lithograph (N. Currier's 1846 "The
      Drunkard's Progress"), then the magazine article, which the instructor
      hands out ON PAPER (it is not carried online). The lithograph comes
      BEFORE the article, since it is immediately relevant to the American Yawp text.
    * Part 4 — a cross-text synthesis (sort "then vs. now"; evaluate peer comparisons).
    * Part 5 — PULLING IT TOGETHER, the culminating synthesis (four or five
      sentences comparing the historic temperance movement with today's
      approaches to drinking less), and a wrap-up.

  Course-team departures from the storyboard:
    * "Joe's notes" (the note-checking matrix) tests the TEXTBOOK.
    * Professor Booker is the READING instructor.

  Layout. The tab strip is the top level, and it is organized around ONE TAB PER
  THING TO GO BACK TO:

    Main Content            the session itself — the scripted conversation on the
                            left, the activity pane (a UseHistory the conversation
                            drives) on the right
    Prof. Hale's Lecture    \
    Textbook 1..4           |  the readings, each on its own tab, so a student can
    The Drunkard's Progress /  reopen any one of them at any point
    Pulling It Together     the culminating task and the material to build it from

  Every reading tab is a <Use ref="..."> of the very same block the conversation
  parks in the activity pane, NOT a second copy: one block, one state, two doors.
  Highlights a student makes on a passage in the flow are the highlights they see
  when they reopen its tab.

  The magazine article has no tab — it is a paper handout (see tmf_article_read).

  Activity screens live in temperance_module_full_activities_part1.olx. Ids are
  prefixed `tmf_` so this build coexists with the `tmod_` faithful build.

  No print="no-chrome" on <Tabs>: this module has no print/submit page yet, so
  there is no export for the tab strip to intrude on.

  How the pane is driven. The conversation opens each activity screen with a
  TARGET EMBED in the chatpeg —

      ::tmf_vks [display=target:tmf_main label="How familiar are these words?"]

  — rather than a bare `tmf_main <- tmf_vks` set command. The screen still lands
  HERE, in the tmf_main pane; what the embed adds is a small labelled button left
  behind in the transcript at the point in the discussion where that activity
  happened. It is a visible marker of where you were, and clicking it repoints
  the pane at that screen again, so going back to an earlier activity is one
  click instead of a scroll-and-guess. The repoint fires once per screen
  (CompactPopout persists a `repointed` flag), so reloading mid-session does not
  drag the pane back to the top of the module.

  The Flash/FocusAction cues below still work unchanged: a target embed writes
  @tmf_main.value exactly as the set command did, so the watch expressions fire
  the same way.

  Only `tmf_main <- tmf_blank` is still a set command — parking the pane empty is
  not an activity, so there is nothing to mark or to return to.
-->

<Tabs id="temperance_module_full_sba" title="Temperance — Full SBA (READ 1000)" launchable="true">

  <!-- ===== Tab 1 (index 0): the session — conversation + activity pane ===== -->
  <Vertical id="tmf_tab_main" title="Discussion">

    <!-- Attention cues. Students don't notice the right pane changing and don't
         know where to look, so every screen the chat opens gets a pulse (Flash)
         as it lands, and the screens with something to fill in also get the
         caret (FocusAction) — which scrolls the control into view for free.
         Reading screens get the pulse alone; the chat points at them in dialogue
         instead (the lead-in lines beside each target embed in the chatpeg).

         Flash aims at the TASK — the Annotate, the grid, the textarea — not at
         the screen's heading. Reading screens have no separate task control, so
         there the target IS the screen (which is the passage).

         These live HERE, outside the SplitPanel, and NOT inside the screens. A
         Trigger's prevValue is persisted component state; a Trigger mounted
         inside a UseHistory screen is unmounted the moment the pane moves on, so
         it never observes its own expression go false and never re-fires.
         Mounted out here it stays alive for the whole session, so mode="each"
         means each visit. -->

    <!-- HACK: the DefinitionKey(...) normalization in these watch expressions is
         NOT long-term stable and should NOT be copied into other courses. It papers
         over the arrow-era assumption that pane values are bare screen names; target
         embeds write qualified refs. The intended fix is cues riding the embed itself
         (flash=/focus= on the ::screen [display=target:...] line) so no pane-value
         comparison exists at all — see bugs.md. We ship this and clean up later. -->
    <Trigger id="tmf_intro_cue"      watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_intro')"      mode="each">
      <Flash id="tmf_intro_flash"      target="tmf_intro" />
    </Trigger>

    <Trigger id="tmf_vks_cue"        watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_vks')"        mode="each">
      <Flash id="tmf_vks_flash"        target="tmf_vks_grid" />
      <FocusAction id="tmf_vks_focus"  target="tmf_vks_grid" />
    </Trigger>

    <!-- The readings. Each is an Annotate (or, for the lithograph, an image);
         the pulse goes on the passage itself, which is the thing to work on. -->

    <!-- Prof. Hale's lecture has no cue: it is an optional reference on its own
         tab now, never parked in the pane, so there is nothing to point at. -->

    <Trigger id="tmf_tb_a_cue"       watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_tb_a')"       mode="each">
      <Flash id="tmf_tb_a_flash"       target="tmf_tb_a_annotate" />
    </Trigger>

    <Trigger id="tmf_tb_a2_cue"      watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_tb_a2')"      mode="each">
      <Flash id="tmf_tb_a2_flash"      target="tmf_tb_a2_annotate" />
    </Trigger>

    <Trigger id="tmf_tb_b_cue"       watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_tb_b')"       mode="each">
      <Flash id="tmf_tb_b_flash"       target="tmf_tb_b_annotate" />
    </Trigger>

    <Trigger id="tmf_tb_c_cue"       watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_tb_c')"       mode="each">
      <Flash id="tmf_tb_c_flash"       target="tmf_tb_c_annotate" />
    </Trigger>

    <!-- The article is read on PAPER, so the only thing on screen to point at is
         the instruction block. The caret is deliberately NOT parked on its Done:
         that is the thing you press after reading, not before. -->
    <Trigger id="tmf_article_cue"    watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_article_read')" mode="each">
      <Flash id="tmf_article_flash"    target="tmf_article_read" />
    </Trigger>

    <Trigger id="tmf_litho_cue"      watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_litho_read')" mode="each">
      <Flash id="tmf_litho_flash"      target="tmf_litho_img" />
    </Trigger>

    <!-- The tasks. -->

    <Trigger id="tmf_notecheck_cue"  watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_notecheck')"  mode="each">
      <Flash id="tmf_notecheck_flash"  target="tmf_notecheck_grid" />
      <FocusAction id="tmf_notecheck_focus" target="tmf_notecheck_grid" />
    </Trigger>

    <!-- Highlighting is worked by dragging over the text and matching by
         clicking pairs, so neither task has a single control to park a caret
         on — pulse only. -->
    <Trigger id="tmf_grammar_svo_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_grammar_svo')" mode="each">
      <Flash id="tmf_grammar_svo_flash" target="tmf_grammar_svo_verb" />
    </Trigger>

    <Trigger id="tmf_grammar_np_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_grammar_np')" mode="each">
      <Flash id="tmf_grammar_np_flash" target="tmf_grammar_np_match" />
    </Trigger>

    <Trigger id="tmf_grammar_structure_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_grammar_structure')" mode="each">
      <Flash id="tmf_grammar_structure_flash" target="tmf_grammar_structure" />
    </Trigger>

    <Trigger id="tmf_grammar_antecedent_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_grammar_antecedent')" mode="each">
      <Flash id="tmf_grammar_antecedent_flash" target="tmf_grammar_antecedent" />
    </Trigger>

    <!-- The comprehension questions are chat-embedded now; the pane holds the
         full excerpt to read from, so this is a reading cue — pulse, no caret. -->
    <Trigger id="tmf_comp_reading_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_comp_reading')" mode="each">
      <Flash id="tmf_comp_reading_flash" target="tmf_comp_reading" />
    </Trigger>

    <Trigger id="tmf_synth_sort_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_synth_sort')" mode="each">
      <Flash id="tmf_synth_sort_flash" target="tmf_synth_sort_grid" />
      <FocusAction id="tmf_synth_sort_focus" target="tmf_synth_sort_grid" />
    </Trigger>

    <Trigger id="tmf_final_write_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_final_write_screen')" mode="each">
      <Flash id="tmf_final_write_flash" target="tmf_final_write" />
      <FocusAction id="tmf_final_write_focus" target="tmf_final_write" />
    </Trigger>

    <Trigger id="tmf_conclusion_cue" watch="DefinitionKey(@tmf_main.value) === DefinitionKey('tmf_conclusion')" mode="each">
      <Flash id="tmf_conclusion_flash" target="tmf_conclusion_md" />
    </Trigger>

    <SplitPanel id="tmf_split" sizes="40, 60">

      <StartPane>
        <!-- height is a FIXED 460px, not the "flex-1" default. This transcript
             runs to well over a hundred turns; with flex-1 there is nothing
             upstream constraining the pane, so the chat simply grew taller with
             every message and pushed the activity pane off the bottom of the
             screen. A fixed box scrolls internally instead, and matches the
             Memphis writing journals. It has to sit on <Chat>, NOT on
             <StartPane>: SplitPanel's parser keeps only the pane tag's children,
             so attributes on the pane tag are silently dropped. -->
        <Chat id="tmf_chat"
              src="temperance_module_full_dialogue_part1.chatpeg"
              cast="characters.cast"
              height="460px" />
      </StartPane>

      <EndPane>
        <UseHistory id="tmf_main" initial="tmf_intro" />
      </EndPane>

    </SplitPanel>
  </Vertical>

  <!-- ===== Tab 2 (index 1): the background lecture ===== -->
  <Vertical id="tmf_tab_lecture" title="Prof. Hale's Lecture">
    <Use ref="tmf_lecture_read" />
  </Vertical>

  <!-- ===== Tabs 3-6 (indices 2-5): the American Yawp excerpt, one tab per
                 part, in reading order. These are the same four Annotate
                 blocks the conversation walks through. ===== -->

  <Vertical id="tmf_tab_textbook_1" title="Textbook 1: Benevolent Empire">
    <Use ref="tmf_tb_a" />
  </Vertical>

  <Vertical id="tmf_tab_textbook_2" title="Textbook 2: Women Reformers">
    <Use ref="tmf_tb_a2" />
  </Vertical>

  <Vertical id="tmf_tab_textbook_3" title="Textbook 3: The Crusade">
    <Use ref="tmf_tb_b" />
  </Vertical>

  <Vertical id="tmf_tab_textbook_4" title="Textbook 4: Class Tensions">
    <Use ref="tmf_tb_c" />
  </Vertical>

  <!-- ===== Tab 7 (index 6): the third text — the 1846 lithograph ===== -->
  <Vertical id="tmf_tab_litho" title="The Drunkard's Progress">
    <Use ref="tmf_litho_read" />
  </Vertical>


</Tabs>
